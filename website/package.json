{
  "scripts": {
    "build": "npm install -g postcss postcss-cli autoprefixer &&  hugo --gc --minify -v -b https://kevinlong.cloud/"
  },
  "dependencies": {
    "autoprefixer": "^10.4.2",
    "postcss-cli": "^9.1.0"
  }
}
